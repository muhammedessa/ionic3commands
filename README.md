# ionic3commands



ionic cordova plugin add cordova-plugin-whitelist

ionic cordova plugin add cordova-plugin-inappbrowser


cordova-plugin-inappbrowser


ionic cordova run android --livereload
{
  "rules": {
    ".read": "auth !=null",
    ".write": "auth !=null"
  }
}



في حالة حروث مشكلة مع npm
npm config set tmp /path/to/some/other/dir


تحديد مسار ال gradle الخاص للاندرويد
export PATH=/opt/android-studio/gradle/gradle-3.2/bin/:$PATH



تحديد مسار ال برنامج اندرويد استوديو 
Linux

export ANDROID_HOME=/home/muhammed/Android/Sdk/
export PATH=${PATH}:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools




Mac OS X

export ANDROID_HOME=/<installation location>/android-sdk-macosx
export PATH=${PATH}:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools




Windows

set ANDROID_HOME=C:\<installation location>\android-sdk-windows
set PATH=%PATH%;%ANDROID_HOME%\tools;%ANDROID_HOME%\platform-tools






















 
